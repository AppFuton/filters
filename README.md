# Storage of filters for Futon app

## Usage

**With Kotlin CLI**
```sh
kotlinc -script extract_tags.main.kts
```

**With IntelliJ IDEA**

1. Open `extract_tags.main.kts`.
2. Run the `Current File` configuration.
